---
title: "Glossary"
description: "Every Claude Code term explained like you're talking to a friend. No jargon. No assumptions. Bookmark this one."
---

## The Jargon-Free Zone

Claude Code has its own vocabulary and honestly some of it sounds more intimidating than it is. This page breaks every term down in plain English.

<DemoCard title="Terms you'll see in action" steps={[
  { type: 'cmd', text: 'claude "Fix the login bug"' },
  { type: 'out', text: '← This is a session (one conversation)' },
  { type: 'out', text: 'Reading CLAUDE.md...' },
  { type: 'out', text: '← CLAUDE.md = your project rules file' },
  { type: 'out', text: 'Spawning code-review agent...' },
  { type: 'out', text: '← Agent = a specialist helper that handles one job' },
  { type: 'out', text: 'Context: 23% used' },
  { type: 'out', text: '← Context window = how much Claude can hold in its head' },
  { type: 'success', text: '✓ Bug fixed in src/auth/login.tsx' },
  { type: 'warn', text: '→ Scroll down for every term explained.' },
]} />

## The Basics

### Claude Code
A command-line tool that lets you work with Claude AI directly in your terminal. Unlike ChatGPT (browser) or Claude.ai (also browser), Claude Code runs right next to your code, your files, and your tools. It can read files, write code, run commands, and actually do things in your project.

### CLAUDE.md
A file at the root of your project that tells Claude Code how to behave. Think of it as a "house rules" document. Your coding standards, communication preferences, workflow patterns. Claude Code reads it at the start of every session. Skip it and Claude Code is guessing. Write a good one and it's like a new team member who read the entire onboarding doc.

### Session
One conversation with Claude Code. You open it, do some work, close it. That's a session. The key thing: without memory and handoffs, every session starts from scratch. With them, sessions build on each other.

### Context Window
How much information Claude can "see" at any one time. Think of it like working memory. A bigger window means it can consider more files and history at once, but it also costs more tokens. When you hear "context is getting full," it means Claude is running out of working memory for this session.

### Prompt
What you type to tell Claude Code what to do. Can be a question, a task, a command, whatever. The quality of your prompt directly affects the quality of the output. But here's the thing most people miss: a good CLAUDE.md matters more than a clever prompt. Context beats cleverness every time.

## Memory and Persistence

### Memory System
A file-based system that lets Claude Code remember things across sessions. Your role, your projects, your preferences, corrections you've made. Without it, every session is a blank slate. With it, Claude Code gets better over time. Like training a team member, except the lessons actually stick.

### Handoff
A document you write at the end of a session that captures what was done, what decisions were made, and what's next. The next session reads this to pick up where you left off. It's literally a note to your future self (and future Claude Code sessions). Takes two minutes, saves ten.

### Product Brain
A directory of living project documents that track each project's current state. Not stale PRDs from six months ago. Living docs that update after every meeting and decision. The source of truth for what's actually happening.

## Automation and Power Features

### Skill
A reusable workflow encoded as a markdown file. Instead of explaining a multi-step task every time, you write it once and trigger it with keywords or commands. Example: a `/meeting-prep` skill that automatically pulls relevant context before any meeting. Skills are the difference between "using AI" and "having a system."

<DemoCard title="Skill in action" steps={[
  { type: 'cmd', text: 'claude "prep for my 1:1 with Sarah"' },
  { type: 'out', text: 'Matched skill: meeting-prep' },
  { type: 'success', text: '✓ Pulling recent Slack threads with Sarah...' },
  { type: 'success', text: '✓ Found 3 open action items from last meeting' },
  { type: 'success', text: '✓ Loaded project context for Dashboard v2' },
  { type: 'out', text: 'Suggested talking points:' },
  { type: 'out', text: '  1. Dashboard deploy blocked on API review' },
  { type: 'out', text: '  2. Her PTO next week — handoff needed' },
  { type: 'out', text: '  3. Open question: Redis vs Edge Config' },
  { type: 'warn', text: '→ One keyword triggered a full workflow. That\'s a skill.' },
]} />

### Hook
A script that runs automatically at specific moments. Before a tool runs, after a tool runs, when a session starts. Like automation rules that fire without you remembering to do anything. Example: auto-format code after Claude writes it. Auto-run security checks before commits.

<DemoCard title="Hook in action" steps={[
  { type: 'cmd', text: 'claude "commit these changes"' },
  { type: 'out', text: 'Staging 3 files...' },
  { type: 'out', text: '[hook: PreToolUse] Running security scan...' },
  { type: 'error', text: '✗ Found hardcoded API key in src/config.ts:14' },
  { type: 'out', text: 'Hook blocked the commit automatically.' },
  { type: 'cmd', text: '← You didn\'t run the check. The hook did.', delay: 1000 },
  { type: 'success', text: '✓ Moved key to .env.local' },
  { type: 'success', text: '✓ Commit succeeded on retry' },
  { type: 'warn', text: '→ Hooks fire without you remembering. Safety net on autopilot.' },
]} />

### Sub-Agent
A separate Claude Code instance that handles one focused task. Your main conversation delegates work to agents that work independently and report back. Research in one agent, code review in another, testing in a third. Keeps your main session clean and focused on decisions.

<DemoCard title="Sub-agents in action" steps={[
  { type: 'cmd', text: 'claude "refactor the auth module"' },
  { type: 'out', text: 'Planning approach...' },
  { type: 'out', text: 'Spawning 3 sub-agents:' },
  { type: 'success', text: '  → Agent 1: Security review of current auth' },
  { type: 'success', text: '  → Agent 2: Write tests for new auth flow' },
  { type: 'success', text: '  → Agent 3: Research best practices (OAuth 2.1)' },
  { type: 'out', text: 'All 3 running in parallel...' },
  { type: 'success', text: '✓ Agent 1 done: 2 vulnerabilities found' },
  { type: 'success', text: '✓ Agent 2 done: 14 test cases written' },
  { type: 'success', text: '✓ Agent 3 done: recommends Clerk + middleware' },
  { type: 'out', text: 'Merging results into refactor plan...' },
  { type: 'warn', text: '→ 3 jobs done in parallel. Main session stayed clean.' },
]} />

### MCP (Model Context Protocol)
A standard for connecting Claude Code to external tools. GitHub, Slack, Jira, databases, whatever. Think of MCP servers as plugins that give Claude Code superpowers beyond just reading and writing files.

### MCP Server
A small program that translates between Claude Code and an external service. Each server gives Claude Code specific abilities. "Read GitHub PRs," "search Slack messages," "create Jira tickets." You configure them in `.mcp.json` and Claude Code uses them when needed.

<DemoCard title="MCP server in action" steps={[
  { type: 'cmd', text: 'claude "what PRs need my review?"' },
  { type: 'out', text: '[MCP: github] Querying pull requests...' },
  { type: 'success', text: '✓ Connected to github.com/acme/webapp' },
  { type: 'out', text: '3 PRs awaiting your review:' },
  { type: 'out', text: '  #142 — Fix auth redirect loop (Sarah)' },
  { type: 'out', text: '  #145 — Add dark mode toggle (Marcus)' },
  { type: 'out', text: '  #147 — Upgrade to Next.js 16 (bot)' },
  { type: 'cmd', text: '"review #142 and leave comments"', delay: 1000 },
  { type: 'out', text: '[MCP: github] Reading PR #142 diff...' },
  { type: 'success', text: '✓ Posted 2 review comments on #142' },
  { type: 'warn', text: '→ Claude Code talked to GitHub directly. That\'s MCP.' },
]} />

## Plans and Pricing

### Claude Pro
$20/month. Good for getting started and moderate daily use. You'll get a solid few hours of active sessions per day. Most people start here and it's genuinely useful.

### Claude Max
The power user plan. $100/month gets you 5x the Pro quota. $200/month gets you 20x. For people who've made Claude Code their primary work tool. If you're hitting usage limits on Pro more than twice a week, this is worth the upgrade.

### Tokens
The unit that measures how much Claude reads and writes. Roughly 4 characters equals 1 token. Your plan has a daily budget. Bigger context windows and longer conversations use more tokens. When you hear "optimize your token usage," it just means "be efficient with Claude's working memory."

### Rate Limit
When you've used your daily token allocation and things slow down. A sign you're either on too small a plan, or you're using context inefficiently (long sessions without clearing, pasting entire files instead of pointing to them).

## Workflow Concepts

### Plan Mode
A mode where Claude Code outlines its approach before doing anything. Instead of immediately writing code (which might be wrong), it proposes a plan first. You review, adjust, then it executes. Saves tokens, prevents wrong-direction work, and honestly just produces better results.

<DemoCard title="Plan mode in action" steps={[
  { type: 'cmd', text: 'claude "add user notifications"' },
  { type: 'out', text: 'Entering plan mode...' },
  { type: 'out', text: '' },
  { type: 'out', text: 'Plan:' },
  { type: 'out', text: '  1. Create NotificationService in lib/' },
  { type: 'out', text: '  2. Add database table for notifications' },
  { type: 'out', text: '  3. Build API route POST /api/notifications' },
  { type: 'out', text: '  4. Add toast component to layout' },
  { type: 'out', text: '  5. Write tests for service + API' },
  { type: 'cmd', text: '"looks good, but skip the toast — use Sonner"', delay: 1200 },
  { type: 'success', text: '✓ Updated plan: using Sonner instead of custom toast' },
  { type: 'success', text: '✓ Executing step 1 of 5...' },
  { type: 'warn', text: '→ Think first, build second. No wasted tokens.' },
]} />

### Autonomous Loop
Claude Code running a task, finishing, and automatically starting the next iteration without your input. Advanced pattern for repetitive tasks like fixing linting errors across a codebase or running through a checklist. Powerful but use with guardrails.

<DemoCard title="Autonomous loop in action" steps={[
  { type: 'cmd', text: 'claude "fix all ESLint errors in src/"' },
  { type: 'out', text: 'Found 23 ESLint errors across 11 files.' },
  { type: 'out', text: 'Starting autonomous fix loop...' },
  { type: 'success', text: '✓ [1/23] Fixed unused import in auth.ts' },
  { type: 'success', text: '✓ [2/23] Fixed missing return type in api.ts' },
  { type: 'success', text: '✓ [3/23] Fixed any → unknown in utils.ts' },
  { type: 'out', text: '...' },
  { type: 'success', text: '✓ [23/23] Fixed missing key prop in UserList.tsx' },
  { type: 'out', text: 'Running ESLint again to verify...' },
  { type: 'success', text: '✓ 0 errors. All clean.' },
  { type: 'warn', text: '→ 23 fixes, zero human input. That\'s an autonomous loop.' },
]} />

### Orchestration
The rules in your CLAUDE.md that govern how Claude Code works. When to plan first. When to use agents. When to verify results. Without orchestration rules, Claude Code is reactive. With them, it's systematic.

## File Types

### `.claude/`
A hidden directory in your project containing Claude Code config: settings, hooks, skills, and agent definitions. Think of it like `.git/` but for Claude Code.

### `MEMORY.md`
The index file for your memory system. Lists all memory files with one-line summaries. Claude Code reads this to know what memories exist and which ones to load.

### `.mcp.json`
Configuration file that defines which MCP servers Claude Code connects to. Where you set up your "plugins" for GitHub, Slack, etc.

### `settings.json`
Claude Code's configuration file at `.claude/settings.json`. Defines hooks, permissions, and tool settings. The mechanical config, as opposed to CLAUDE.md which is the behavioral config.
