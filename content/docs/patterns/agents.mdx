---
title: Sub-Agents
description: "Stop burning your main context on 200-line code reviews. Delegate to focused agents, get the summary back."
---

## Your Main Conversation Is Not a Dumpster

Here's what happens without sub-agents: you ask Claude Code to review your auth changes, it reads 15 files, writes a 200-line audit, and now your context window is half gone. You needed the 5-line summary. Not the journey.

Sub-agents fix this. They're separate Claude Code instances that handle focused tasks in their own context window. The security review happens over there. The test generation happens over there. Your main session stays clean.

<DemoCard title="Sub-agents working in parallel" steps={[
  { type: 'cmd', text: 'claude "ship the new auth feature"' },
  { type: 'out', text: 'This is a multi-step task. Entering plan mode...' },
  { type: 'out', text: 'Spawning 3 agents in parallel:' },
  { type: 'success', text: '  → code-reviewer: reviewing auth changes' },
  { type: 'success', text: '  → security-reviewer: checking for vulnerabilities' },
  { type: 'success', text: '  → tdd-guide: writing missing tests' },
  { type: 'out', text: 'Working...' },
  { type: 'success', text: '✓ code-reviewer: 2 style fixes applied' },
  { type: 'success', text: '✓ security-reviewer: LGTM, no issues' },
  { type: 'success', text: '✓ tdd-guide: 8 tests added, all passing' },
  { type: 'out', text: 'All agents complete. Ready to commit.' },
  { type: 'warn', text: '→ 3 expert reviews in the time of 1. Main session stayed clean.' },
]} />

## Why This Is a Big Deal

**Your context stays focused.** The main session handles decisions. Agents handle the deep dives. You get the result without the noise.

**Parallel execution.** Need a code review, security scan, and test generation? That's three agents working simultaneously, not one sequential chain. Three expert reviews in the time of one.

**Specialization.** Each agent carries instructions tuned for its job. A code-reviewer knows your team's standards. A tdd-guide enforces test-first. Better instructions, better output.

## Built-in Agent Types

Claude Code ships with several agent patterns ready to go:

| Agent | What It Does | When to Use |
|---|---|---|
| **code-reviewer** | Reviews code for quality, patterns, bugs | After writing or modifying code |
| **tdd-guide** | Enforces test-driven development | New features, bug fixes |
| **planner** | Creates implementation plans | Complex features, refactoring |
| **security-reviewer** | Scans for vulnerabilities | Before commits, auth changes |
| **build-error-resolver** | Diagnoses and fixes build failures | When the build breaks |

These spawn automatically when the task calls for it. You can also be explicit: "Run the code-reviewer on the files I just changed."

## Building Your Own Agents

Custom agents are markdown files in `.claude/agents/`. Each one defines a role, behavior, and constraints.

```markdown
# PR Description Writer

## Role
You generate pull request descriptions from git diffs.

## Behavior
1. Read the full diff with `git diff main...HEAD`
2. Analyze each commit for its purpose
3. Group changes by category (feature, fix, refactor, test)
4. Write a structured PR description:
   - Summary (2-3 sentences)
   - Changes (bulleted by category)
   - Testing notes
   - Breaking changes (if any)

## Constraints
- Never include file paths with absolute user directories
- Keep the summary under 100 words
- Use conventional commit categories
```

Save it, then invoke it: "Use the PR description writer for this branch." Claude Code reads the file, follows the instructions, delivers the output. That simple.

## When to Delegate vs Just Do It

Not everything needs an agent. Here's the honest breakdown:

**Delegate when:**
- The task requires reading a ton of files (research, audits)
- You need the result but don't care about the process
- Multiple tasks can run in parallel
- It's a review, scan, or verification task

**Just do it directly when:**
- It's a quick edit (a few lines)
- You need to steer every step
- It's a one-off question about the codebase

The rule of thumb: if it takes more than a minute and you don't need to babysit it, delegate.

## Parallel Execution Is the Real Power

For complex tasks, launch multiple agents at once:

```markdown
Launch 3 agents in parallel:
1. Agent 1: Security review of the auth module
2. Agent 2: Performance audit of the database queries
3. Agent 3: Test coverage analysis for the API routes
```

Each works independently, uses its own context, returns a focused report. This is where sub-agents go from "nice to have" to "how did I work without this."

## Tips from the Trenches

**One task per agent.** An agent that does code review AND writes tests is two agents crammed into one. Split them.

**Be specific.** "Review this file for security issues" works way better than "look at the code and tell me if anything's wrong." Bounded tasks, better results.

**Trust but verify.** Agents are good, not perfect. Skim the output before acting. False positives happen. Edge cases get missed.

**Start with the built-ins.** Use code-reviewer and tdd-guide for a week before writing custom agents. You'll learn what you actually need.

**Keep agent files short.** 20-40 lines is the sweet spot. If your agent file hits 200 lines, you're encoding multiple agents into one.
