---
title: Hooks
description: "Stop remembering to format, lint, and check for secrets. Configure it once, never think about it again."
---

## Why Do You Keep Forgetting?

Every time you tell Claude Code "remember to run Prettier after editing" or "always check for secrets before committing," you're wasting a prompt. You're also going to forget eventually.

Hooks fix this permanently. They're scripts that fire automatically before a tool runs, after a tool runs, or when a session starts. You set them up once and they work silently forever.

Think of them like git hooks, but for Claude Code. And honestly? They're more useful than most git hooks you've written.

<DemoCard title="Hooks working silently in the background" steps={[
  { type: 'cmd', text: 'claude "add a new API endpoint for users"' },
  { type: 'out', text: 'Writing src/app/api/users/route.ts...' },
  { type: 'out', text: '[PostToolUse] Running prettier --write...' },
  { type: 'success', text: '✓ Auto-formatted route.ts' },
  { type: 'out', text: '[PostToolUse] Running eslint --fix...' },
  { type: 'success', text: '✓ Lint clean' },
  { type: 'cmd', text: 'claude "commit this"', delay: 1000 },
  { type: 'out', text: '[PreToolUse] Scanning for secrets...' },
  { type: 'error', text: '✗ Blocked: hardcoded DB_URL in route.ts:3' },
  { type: 'out', text: 'Moving to .env.local...' },
  { type: 'success', text: '✓ Secret removed. Commit succeeded.' },
  { type: 'warn', text: '→ You configured this once. It runs forever.' },
]} />

## The Three Hook Types

### PreToolUse

Fires **before** Claude Code does something. This is your gatekeeper.

Use it to block writes to protected files, catch secrets before they land in code, or add a confirmation step before anything destructive.

### PostToolUse

Fires **after** Claude Code does something. This is your cleanup crew.

Auto-format code after every edit. Run a linter on new files. Log tool usage for auditing. The stuff you'd otherwise have to nag about.

### SessionStart

Fires **when a session begins**. This is your context loader.

Remind Claude Code to read memory files. Check that env vars are set. Load the latest handoff so you never start cold.

## How to Configure Them

Hooks live in your `.claude/settings.json`. Each one says when to fire and what to run.

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hook": "echo 'Checking file write...'"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hook": "npx prettier --write $CLAUDE_FILE_PATH"
      }
    ],
    "SessionStart": [
      {
        "hook": "echo 'Remember: Read MEMORY.md and check handoffs/ for context.'"
      }
    ]
  }
}
```

The `matcher` field controls which tools trigger the hook. Pipe-separate tool names like `"Write|Edit"` to match multiple. Leave it out and the hook fires on every tool call.

## Your First Hook (30 Seconds, Huge Payoff)

The single highest-value hook you can add right now ensures Claude Code never starts a session cold.

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hook": "cat ~/.claude/session-reminder.txt 2>/dev/null || echo 'No session reminder found.'"
      }
    ]
  }
}
```

Create `~/.claude/session-reminder.txt` with your startup checklist:

```text
Session startup checklist:
1. Read MEMORY.md for persistent context
2. Check _context/handoffs/ for the latest handoff
3. Resume from where the last session left off
4. If no handoff exists, ask what we're working on today
```

No more "wait, you forgot about what we did yesterday." Done.

## Auto-Format on Every Edit

This is the second hook everyone should have. Prettier runs after every file edit so formatting is never a conversation:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hook": "npx prettier --write $CLAUDE_FILE_PATH 2>/dev/null || true"
      }
    ]
  }
}
```

The `|| true` keeps things moving if Prettier can't handle a file type. Always add a fallback for non-critical hooks.

<DemoCard title="SessionStart hook: never start cold again" steps={[
  { type: 'cmd', text: 'claude' },
  { type: 'out', text: '[SessionStart] Loading context...' },
  { type: 'success', text: '✓ Read MEMORY.md (3 projects, 8 preferences)' },
  { type: 'success', text: '✓ Read handoffs/2026-04-08.md' },
  { type: 'success', text: '✓ Loaded: "Dashboard feature — tooltip PR pending review"' },
  { type: 'out', text: '' },
  { type: 'cmd', text: '"continue where we left off"', delay: 600 },
  { type: 'out', text: 'Resuming dashboard tooltips. Reading PR #142 comments...' },
  { type: 'success', text: '✓ Addressed 2 review comments. Updated tests.' },
  { type: 'warn', text: '→ Zero re-explanation. The hook loaded everything.' },
]} />

## Tips That Actually Matter

**Keep hooks fast.** They block the action they're attached to. A 10-second hook means Claude Code pauses for 10 seconds before every tool call. Under 1 second is the target.

**Test with echo first.** Before wiring up a real script, use `echo` to verify the hook fires when you expect. Once confirmed, swap in the real command.

**Don't go overboard.** Three or four well-chosen hooks beat twenty noisy ones. Start with a SessionStart reminder and one PostToolUse formatter. Add more only when you feel friction.

**Scope them right.** Hooks in `.claude/settings.json` at the project level only fire for that project. Hooks in `~/.claude/settings.json` fire globally. Project-specific formatting goes at the project level. Universal safety checks go global.

**Handle failures gracefully.** A crashing hook can block your entire workflow. Always add `2>/dev/null || true` for non-critical hooks, and test commands independently before adding them to config.
